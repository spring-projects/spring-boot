---
platform: linux
inputs:
  - name: git-repo
outputs:
  - name: docker-updates-git-repo
params:
  GITHUB_REPO:
  GITHUB_ORGANIZATION:
  GITHUB_PASSWORD:
  GITHUB_USERNAME:
  ISSUE_TITLE:
run:
  path: git-repo/ci/scripts/detect-docker-updates.sh
