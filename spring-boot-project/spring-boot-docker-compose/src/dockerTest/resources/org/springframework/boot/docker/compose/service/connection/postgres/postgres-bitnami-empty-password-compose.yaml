services:
  database:
    image: '{imageName}'
    ports:
      - '5432'
    environment:
      - 'POSTGRESQL_USERNAME=myuser'
      - 'POSTGRESQL_DATABASE=mydatabase'
      - 'ALLOW_EMPTY_PASSWORD=yes'
